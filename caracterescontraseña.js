let pass = prompt("digitela contraseña");
let numCaracteres = pass.length;
console.log(numCaracteres);
if (numCaracteres >= 8 && numCaracteres <=12){

    alert ("la contraseña es valida");
} else {
alert ("la contraseña no es valida");

}
